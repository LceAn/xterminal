# 未来更新计划（Roadmap）

> 最近更新：2026-08-30。单文件 Go 监控面板，计划保持"零依赖、单二进制"形态。

## 短期

- 可选访问令牌（`--token`），用于必须暴露到内网其他主机的场景。
- `/api` 响应增加采集耗时字段，便于判断卡顿来源。

## 中期

- 前端自动刷新间隔可配置（当前手动刷新）。
- 磁盘 IO 指标（/proc/diskstats）补充到面板。

## 长期 / 想法

- systemd 单元文件样例直接入库（README 中的示例固化为 `deploy/xterminal.service`）。
